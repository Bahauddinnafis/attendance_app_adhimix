name: absensi_adhimix
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  date_picker_timeline: ^1.2.6
  firebase_core: ^3.1.0
  firebase_auth: 5.1.0
  fluttertoast: ^8.2.6
  cloud_firestore: ^5.0.1
  google_sign_in: ^6.2.1
  geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
